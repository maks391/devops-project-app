# VAŽNO: Zamijeni placeholder vrijednosti prije primjene!
# Generiraj base64: echo -n "tvoja-lozinka" | base64
#
# Primjer postavljanja bez uređivanja datoteke (preporučeno):
#   kubectl create secret generic ticketing-secrets \
#     --from-literal=POSTGRES_PASSWORD=<prod-lozinka> \
#     --from-literal=POSTGRES_USER=ticketing_user \
#     --from-literal=POSTGRES_DB=ticketing \
#     -n ticketing
apiVersion: v1
kind: Secret
metadata:
  name: ticketing-secrets
  namespace: ticketing
type: Opaque
data:
  # "change_me_prod" — ZAMIJENI u produkciji
  POSTGRES_PASSWORD: Y2hhbmdlX21lX3Byb2Q=
  # "ticketing_user"
  POSTGRES_USER: dGlja2V0aW5nX3VzZXI=
  # "ticketing"
  POSTGRES_DB: dGlja2V0aW5n
---
# Secret za pull iz GHCR registra
# Kreiraj ručno (PAT nije za verzioniranje):
#   kubectl create secret docker-registry ghcr-secret \
#     --docker-server=ghcr.io \
#     --docker-username=<github-username> \
#     --docker-password=<github-pat> \
#     -n ticketing
